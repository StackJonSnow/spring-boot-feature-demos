spring.datasource.url=jdbc:mysql://localhost:3306/security_demo
spring.datasource.username=root
spring.datasource.password=mysql@2020
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#spring jpa 配置
spring.jpa.show-sql=true


redis:
host: 127.0.0.1
port: 6379
password: springuseful123456
jedis:
pool:
max-idle: 8
min-idle: 0
max-active: 8
max-wait: -1
timeout: 3000

# 指定 session 保存
session:
store-type: redis
redis:
flush-mode: on_save
namespace: spring:session